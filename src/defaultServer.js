// Endereço padrão da API — usado só se o usuário nunca configurou um servidor no app.
// Troque aqui quando trocar o túnel do ngrok (ou apontar pra um domínio fixo depois).
// Também dá pra mudar dentro do app, em Ajustes → Servidor (o que for salvo lá tem prioridade).
export const DEFAULT_SERVER_URL = 'https://0310-170-81-63-98.ngrok-free.app';
